<h1>Listing criminal_groups</h1>

<table>
  <tr>
    <th>Id</th>
    <th>Description</th>
    <th>Vehicle</th>
    <th>Weapon</th>
    <th>Drug</th>
    <th>Area</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @criminal_groups.each do |criminal_group| %>
  <tr>
    <td><%= criminal_group.id %></td>
    <td><%= criminal_group.description %></td>
    <td><%= criminal_group.vehicle_id %></td>
    <td><%= criminal_group.weapon_id %></td>
    <td><%= criminal_group.drug_id %></td>
    <td><%= criminal_group.area_id %></td>
    <td><%= link_to 'Show', criminal_group %></td>
    <td><%= link_to 'Edit', edit_criminal_group_path(criminal_group) %></td>
    <td><%= link_to 'Destroy', criminal_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Criminal group', new_criminal_group_path %>
